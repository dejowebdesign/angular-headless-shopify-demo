server {
    listen 80;
    server_name localhost;
    
    location / {
        try_files \$uri \$uri/ /index.html;
    }
    
    # Handle Angular routing, e.g., /shop-details/:id
    location ~ ^/(shop-details/[^/]+|shop/[^/]+) {
        try_files \$uri \$uri/ /index.html;
    }
}
